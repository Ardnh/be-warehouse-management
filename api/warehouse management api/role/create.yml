info:
  name: create role
  type: http
  seq: 3
http:
  method: POST
  url: "{{base_url}}/role/"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "code": "WAREHOUSE_OPERATOR",
        "name": "Warehouse operator",
        "description": "Warehouse operations role",
        "status": "ACTIVE"
      }
