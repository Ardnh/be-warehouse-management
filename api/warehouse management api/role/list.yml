info:
  name: list roles
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/role/?page=1&page_size=30&search="
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
