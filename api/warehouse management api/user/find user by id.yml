info:
  name: find user by id
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/user/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
