info:
  name: get profile
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/user/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
