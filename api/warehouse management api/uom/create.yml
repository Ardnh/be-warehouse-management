info:
  name: create UOM
  type: http
  seq: 3
http:
  method: POST
  url: "{{base_url}}/uom/"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "code": "PCS",
        "name": "Pieces",
        "type": "COUNT"
      }
