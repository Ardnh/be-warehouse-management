info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/login"
  body:
    type: json
    data: |-
      {
        "username": "system-admin",
        "password": "123123"
      }

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("token", res.body.data.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
