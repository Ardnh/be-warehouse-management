info:
  name: list inbound orders
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/inbound-order/?page=1&page_size=30&search="
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
